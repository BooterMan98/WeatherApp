# Realtime simulator Weather App

# Instructions:

- To run main app
  ```powershell
  dotnet build --project WeatherApp
  dotnet run --project WeatherApp
  ```
- To run tests:
  ```powershell
  dotnet build --project WeatherAppTests
  dotnet test
  ``` 